# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project

This is a PHP project managed with Composer (configured in PhpStorm/.idea). The project is in early setup — no source code exists yet.

## Getting Started

Once dependencies and source code are added, common commands will include:

```bash
composer install        # install PHP dependencies
composer dump-autoload  # regenerate autoloader
```
